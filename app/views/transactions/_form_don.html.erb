<%= simple_form_for(@transaction) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.hidden_field :from,:value => 'don' %>
    <%= f.association :mort %>
    <%= f.association :donnateur %>
    <%= f.input :montant %>
    <%= f.input :numero_de_piece %>
    <%= f.input :commentaire %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: ['btn', 'btn-primary'], :value => 'Faire un don' %>
  </div>
<% end %>
