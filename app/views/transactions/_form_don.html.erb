<%= simple_form_for(@transaction) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<%# TODO : label a modifier en 'personne decedee'%>	
    <%= f.association :mort %>
    <%= f.association :donnateur %>
	<%# TODO : label a modifier en 'Nombre de messes'%>
    <%= f.input :montant %>
    <%= f.input :numero_de_piece %>
    <%= f.input :commentaire %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: ['btn', 'btn-primary'], :value => 'Faire un don' %>
  </div>
<% end %>
